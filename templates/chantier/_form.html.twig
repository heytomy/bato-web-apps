<div class="container-fluid">

	{# <div class="row mb-4">
		<div class="col d-flex justify-content-end">
			<button class="btn btn-secondary" id="clear-fields-btn">Réinitialiser</button>
		</div>
	</div> #}

	<div class="card border rounded-5 bg-light p-4 mt-4">
		{{ form_start(form) }}

		<div class="row mt-3">
        	<div class="col">{{ form_row(form.ID_Utilisateur) }}</div>
		</div>

		<div class="row mt-3">
        	<div class="col">{{ form_row(form.codeClient) }}</div>
		</div>

		<div class="row mt-3 ">
            <div class="col">{{ form_row(form.libelle) }}</div>
		</div>

		<div class="row mt-3">
			<div class="col-md-6 mt-1">{{ form_row(form.adresse) }}</div>
			<div class="col-md-2 mt-1">{{ form_row(form.cp) }}</div>
			<div class="col-md-4 mt-1">{{ form_row(form.ville) }}</div>
		</div>

		
		<div class="row mt-1">
			<div class="col-md-6">{{ form_row(form.Tel) }}</div>
			<div class="col-md-6">{{ form_row(form.Email) }}</div>
		</div>


		<div class="row mt-4">
			<div class="col">{{ form_row(form.description) }}</div>
		</div>

		<div class="row mt-3">
			<div class="col-md-6">{{ form_row(form.dateDebut) }}</div>
			<div class="col-md-6">{{ form_row(form.dateFin) }}</div>
		</div>

		<button type="submit" class="btn btn-primary mt-3">{{ button_label|default('Enregistrer') }}</button>
		{{ form_end(form) }}
	</div>
</div>
