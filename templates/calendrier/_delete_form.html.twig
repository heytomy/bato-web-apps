<form method="post" action="{{ path('app_calendrier_delete', {'id': calendrier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendrier.id) }}">
    <button class="btn">Delete</button>
</form>
