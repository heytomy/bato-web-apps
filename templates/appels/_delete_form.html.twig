<form method="post" action="{{ path('app_appels_delete', {'id': appels.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appels.id) }}">
    <button class="btn">Supprimer</button>
</form>
